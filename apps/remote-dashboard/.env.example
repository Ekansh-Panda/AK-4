# Miori remote dashboard — environment example.
# Copy to `.env` for local overrides. All exposed vars must be prefixed VITE_.

# Default host address pre-filled on the login screen (optional).
# VITE_DEFAULT_HOST=http://192.168.1.20:8000
