/**
 * Mock data has been removed. The desktop app now renders honest "not
 * connected" / empty states from real backend responses instead of fixtures.
 *
 * This file is intentionally empty (kept only as a module stub); it can be
 * deleted from disk safely — nothing imports it.
 */
export {};
