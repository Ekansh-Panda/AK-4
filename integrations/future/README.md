# integrations/future

Placeholder for donor repos harvested in later phases (see `TASKS.md` v0.3):

- **Computer-use / action-loop** projects → `services/core-api/app/services/tools/computer_use/`
- **Agent orchestration** projects → `services/core-api/app/services/tools/` + a future orchestrator
- **Voice pipeline** donors → voice services (deferred)

Clone here as `integrations/future/<name>` and document in
`docs/repo-analysis/`. Do **not** import their architecture wholesale — port
patterns only, per the harvest rules in `integrations/README.md`.
